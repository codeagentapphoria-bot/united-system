# Defaults to "/api" in code if left empty — no change needed for production
# (frontend and backend are served from the same origin via merge-port).
VITE_API_BASE_URL=http://localhost:5000/api

VITE_EXTERNAL_API_URL=
VITE_EXTERNAL_API_KEY=

VITE_SMTP_FROM=noreply@bims.com
