fix: drop eservice_users.role column from e-service user accounts table

The 'Role' column showed the generic eservice_users.role flag (always
'admin') which was confusing. Now table shows only: Name, Email,
System Roles (actual role names), Created.
