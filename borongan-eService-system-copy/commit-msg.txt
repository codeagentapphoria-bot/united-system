fix: only show admin-role users in systems list and fix badge readability

- Backend: filter getSuperAdminDashboard userRoles to only include
  users where eservice_users.role = 'admin', excluding staff/non-admin accounts
- Frontend: replace Badge component (secondary variant too subtle on white card)
  with inline styled spans using bg-primary-50 border-primary-200 for
  primary-colored badges on all role/admin count badges
